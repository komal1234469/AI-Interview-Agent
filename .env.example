# Optional — the agent works fully without either of these (deterministic
# engine only). Set one to enable LLM-phrased questions/follow-ups.

ANTHROPIC_API_KEY="your anthropic api key"
ANTHROPIC_MODEL=claude-sonnet-4-6

OPENAI_API_KEY="Your open ai api key"
OPENAI_MODEL=gpt-4o-mini

PORT=3000